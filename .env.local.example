# Enable demo fallback mode for local UI testing (default: disabled)
# VITE_ENABLE_DEV_FALLBACK=true

# Optional: force API base URL in frontend
# VITE_API_BASE_URL=https://labelhub-backend.onrender.com/api
